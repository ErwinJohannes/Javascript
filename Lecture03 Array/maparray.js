const finalParticipants = ["Taylor","Donald","Don","Eren","Bubba"];

const announcements = finalParticipants.map((member) => {
    return member + "joined the arena.";

});

console.log(announcements);