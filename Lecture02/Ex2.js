function equation(cal) {
    cal.split('').forEach((char) =>{
        //console.log(char);
        switch(char) {
            case '+':
                console.log('this +');
                break;
}